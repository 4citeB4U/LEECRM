/**
 * Environment Variables for LEEWAY™ CRM System
 * 
 * INSTRUCTIONS:
 * 1. Copy this file to .env.js
 * 2. Fill in your API keys and other environment variables
 * 3. Keep .env.js out of version control (add to .gitignore)
 * 
 * SECURITY NOTES:
 * - API keys stored here will be loaded into the browser
 * - For production, consider using server-side environment variables
 * - Use API key restrictions (domain, IP, etc.) when possible
 */

window.env = {
  // General configuration
  APP_NAME: 'Agent Lee CRM',
  APP_VERSION: '1.0.0',
  
  // API Keys
  EMAILJS_USER_ID: 'your_emailjs_user_id',
  EMAILJS_SERVICE_ID: 'your_emailjs_service_id',
  EMAILJS_TEMPLATE_ID: 'your_emailjs_template_id',
  
  GOOGLE_MAPS_API_KEY: 'your_google_maps_api_key',
  GA_TRACKING_ID: 'your_google_analytics_tracking_id',
  
  GEMINI_API_KEY: 'your_gemini_api_key',
  OPENAI_API_KEY: 'your_openai_api_key',
  
  // Feature flags
  ENABLE_VOICE_DICTATION: true,
  ENABLE_AI_ASSISTANT: true,
  ENABLE_ANALYTICS: true,
  ENABLE_MAPPING: true,
  
  // Default location (Milwaukee)
  DEFAULT_LOCATION: { lat: 43.0389, lng: -87.9065 },
  
  // Security settings
  ENCRYPT_STORED_DATA: true,
  API_KEY_ROTATION_DAYS: 30
};
